#include<stdio.h>
#include<string.h>
#include<stdlib.h>
void strict(FILE *f, char *wrd)
{
  char str[100],ch;
  win
  if(f==NULL)
    exit(0);
  else
  {
  while(f!=EOF)
  {
    fgets(str,100,f);
    fscanf(str,%s,
  }
  }
  
}
